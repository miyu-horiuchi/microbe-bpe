# Evo2 feature extraction — install ONLY on a CUDA GPU box (Lambda / RunPod /
# Colab A100, etc). Evo2 does not run on macOS or CPU.
#
#   evo2_7b      -> runs in bfloat16 on any supported CUDA GPU (~15 GB)
#   evo2_1b_base -> needs FP8 / Transformer Engine on a Hopper GPU (H100/H200)
#
# Prereqs on the box: CUDA 12.1+, a recent NVIDIA driver, and (for FP8 models)
# NVIDIA Transformer Engine. See https://github.com/ArcInstitute/evo2 for the
# authoritative, up-to-date install instructions; pin nothing here that the
# upstream installer manages itself.

evo2            # Arc Institute Evo 2 (pulls vortex / flash-attn as needed)
numpy>=1.26,<2
pandas>=2.0
pyarrow>=15
tqdm>=4.66
biopython>=1.83
requests>=2.32
